---
import { PAGE_TITLE } from '~/constants';

export type Props = {
    isTop: boolean;
};
const { isTop } = Astro.props;
---

<nav
    class="custom-shadow grid h-14 items-center rounded-full border-2 border-solid border-primary bg-background xs:h-16 sm:h-20"
>
    {
        isTop ? (
            <h1 class="title">{PAGE_TITLE}</h1>
        ) : (
            <a class="title" href="/">
                {PAGE_TITLE}
            </a>
        )
    }
</nav>
<style>
    .title {
        @apply pl-5 text-2xl text-primary xs:pl-6 xs:text-3xl sm:pl-8 sm:text-[40px];
    }
</style>
