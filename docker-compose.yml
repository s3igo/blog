services:
  frontend:
    container_name: blog-frontend
    build:
      context: .
      dockerfile: ./infra/develop/Dockerfile
    tty: true
    ports:
      - 3000:3000
    volumes:
      - .:/app
    working_dir: /app/frontend
    command: bash -c "npm install && npm run dev"
