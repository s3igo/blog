const manifestPathname = '/manifest.webmanifest';
const faviconPathname = '/favicon.ico';
const sitemapPathname = '/sitemap-index.xml';
const rssPathname = '/rss.xml';

export const pathnames = {
    manifestPathname,
    faviconPathname,
    sitemapPathname,
    rssPathname,
};
