---
import {
    pathnames,
    postPathnameBuilder,
    tagPathnameBuilder,
} from '../layers/app/routes/index.ts';
import FeedPage from '../layers/pages/feed/FeedPage.astro';

const props = {
    cardPathnameBuilder: postPathnameBuilder,
    tagPathnameBuilder,
    ...pathnames,
};
---

<FeedPage { ...props } />
